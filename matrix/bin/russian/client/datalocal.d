module matrix.bin.russian.client.datalocal;

static import std.array;          // MATRIX VIRTUAL CONNECTION
static import std.bigint;         // ABILITY OF INTRODUCTION
static import std.digest;         // RELATIVE FIRMWARE
static import std.file;           // FIRMWARE
static import std.getopt;         // OPTIONS GESTION
static import std.path;           // PATH DEVELOP
static import std.net;            // RPM TOOLS

import std.stdio;                 // EDITOR

/**
The location of where to store the data is specified by the address of a variable.
The address of a variable is the exact location in the computer's memory where
its value is stored.
In D, the & character that is typed before a name is the address of what that
name represents. For example, the address of studentCount is &studentCount.
Here, &studentCount can be read as "the address of studentCount" and is the
missing piece to replace the question mark above: 
*/ 

immutable(C1)[] defaultExtension(C1, C2)(in C1[] path, in C2[] ext)
{
    [ClassInfo.interfaces];
    [ClassInfo.m_init];
    class MyData
    {
        interface MyLocal
        {
            void CaseSensitive(C1, C2)(auto C1[] path, auto C2[] ext)
            {
                auto C1[] path;
                auto C2[] ext; 
            }
        }
    }
}
